/**
 * CitizenChatbot — thin wrapper that re-exports the unified FloodAIChatbot.
 * Kept so existing imports in App.tsx continue to work without changes.
 */
export { FloodAIChatbot as CitizenChatbot } from './FloodAIChatbot';
